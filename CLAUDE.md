# CLAUDE.md — Mirrors AGENTS.md for Claude Code

This repository's AI workflow is defined in `AGENTS.md`.
Claude Code must read `AGENTS.md` in full before any code task and follow all rules therein (relay overlay, single source `relay.config.json`, `sync-relay.mjs`, `merge=ours`, build/verify steps).

Detailed guide: `docs/RELAY_GUIDE.md`.
